# TS-007 – Analytics tracking for key actions

Verify that key user interactions trigger analytics events.

High-level flow:
1. User clicks primary CTA
2. User navigates service pages
3. Events are tracked correctly
