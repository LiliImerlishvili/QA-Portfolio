# TS-009 – Cross-browser compatibility

Verify consistent UI and behavior across browsers.

High-level flow:
1. User opens site in Chrome, Safari, Firefox, Edge
2. User navigates main pages
3. UI remains consistent
