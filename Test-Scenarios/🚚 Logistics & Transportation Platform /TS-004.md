# 🎬 TS-004 – Localization and multi-language behavior

This scenario validates localization behavior across key platform pages.

It covers:
- Language switching across listing, booking, and tracking pages
- Translation completeness of UI and dynamic content
- Data consistency when language changes mid-flow
- Format validation (dates, currency, addresses) per locale
