# 🎬 TS-008 – Session and state persistence in active flows

This scenario validates that user session and in-progress flows remain stable.

It covers:
- Preserving booking state across page refresh
- Handling session expiration gracefully
- Clear recovery path when session is invalid
- Preventing duplicate submissions on retry
